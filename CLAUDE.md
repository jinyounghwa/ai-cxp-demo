# CLAUDE.md — AI-CXP (AI 기반 통합 고객경험 플랫폼) 데모

## 슬로건
"대화 한 번으로 상담부터 구매까지 — 검증된 AI로 신뢰를 더하다"

## 목표
콜센터/영업/커머스가 분리 운영되던 기존 엔터프라이즈 구조를,
**챗봇·콜봇 하나의 대화 채널**에서 CRM 데이터와 커머스 트랜잭션까지
연결해 처리하고, 그 응답을 **RAG/sLLM 환각검증**으로 신뢰도까지 보장하는
데모 시스템을 구축한다. 목적은 "AI가 실제 업무를 대신 처리하되,
틀린 답을 하지 않는다"는 것을 시연 가능한 형태로 증명하는 것.

## 사용자
| 사용자 | 니즈 |
|---|---|
| 콜센터/채팅 상담사 | 반복 문의 자동 응대, 어려운 건만 이관받기 |
| 영업담당자 | 리드/고객 이력 조회, 다음 액션 추천 |
| 커머스 MD/운영자 | 상품 문의 자동 응대, 주문 연동 확인 |
| 최종 고객 | 챗봇/콜봇으로 상담·구매·AS 요청 |
| AI/데이터 운영자 | 인텐트·엔티티 학습 현황, 환각률 모니터링 |

## 핵심기능 (5대 모듈)
1. **챗봇/콜봇**: 텍스트 챗봇 + STT/TTS 기반 콜봇, 동일 대화엔진 공유
2. **CRM (영업관리/영업지원)**: 고객·리드·상담이력·영업활동 관리, 챗봇과 데이터 연동
3. **E-커머스**: 상품/주문/결제 데모, 챗봇에서 상품 문의→주문까지 연결
4. **AI 인텐트/엔티티 학습**: 발화 → 의도분류 + 개체추출 파이프라인, 학습 데이터 관리 UI
5. **RAG/sLLM 환각검증**: 답변 근거(citation) 대조, 환각 스코어링, 검증 테스트 화면

## 스코프
**포함**
- 5대 모듈의 데모 수준 End-to-End 플로우 (실제 PG/통신사 연동 X, Mock)
- 인텐트 20~30개, 엔티티 10종 내외의 학습 데이터셋
- RAG 파이프라인 + 환각검증 대시보드(자동채점 + 사람 검수 병행)
- 콜봇은 STT/TTS Mock 또는 오픈소스 엔진으로 대체 (실제 통신망 연동 제외)

**제외 (데모 범위 밖)**
- 실결제/실통화 연동, 다국어, 대규모 트래픽 처리
- 실제 개인정보/고객 데이터 (더미 데이터만 사용)
- 상담사 배정 알고리즘 고도화, 옴니채널 전체(이메일/SMS 등)

## 성공기준
1. 챗봇 대화 1회 안에서 **CRM 조회 → 상품 추천 → 주문 생성**까지 끊김 없이 시연
2. 콜봇 음성 입력 → 인텐트 분류 → CRM/커머스 액션 실행까지 재현 가능
3. 신규 발화 등록 시 인텐트/엔티티 학습 데이터에 반영되고 재학습 트리거 확인 가능
4. RAG 답변에 대해 **환각 여부(신뢰/주의/오류)가 화면에 표시**되고, 근거 문서 하이라이트 제공
5. 환각검증 테스트셋(최소 30문항) 실행 결과가 정확도/환각률 수치로 리포트됨

## 기술스택 (요약 — 세부는 SKILL.md 참조)
- Frontend: React + Vite + TypeScript
- Backend: NestJS (API Gateway) + TypeScript
- AI Layer: sLLM(로컬/오픈소스) + RAG(Vector DB) + 인텐트/엔티티 분류기
- DB: PostgreSQL(CRM/커머스) + Vector DB(RAG) + Redis(세션/캐시)
- 통신 데모: WebSocket(챗봇), STT/TTS 오픈소스 or Mock(콜봇)

## 진행 방식
- 본 문서(CLAUDE.md)는 항상 200줄 이하로 유지, 방향성/스코프만 관리
- 모듈별 구현 세부사항, API 스펙, 테스트 시나리오는 `SKILL.md`에서 관리
- 스프린트 단위 진행 시 Sprint Plan을 별도 문서로 분리 (SGD 방식)
